in terminal:

python -m venv .venv
# Activate the venv (PowerShell):
. .venv\Scripts\Activate.ps1
# Or CMD:
# .venv\Scripts\activate.bat

python -m pip install --upgrade pip
pip install ultralytics opencv-python pywin32 dxcam